name: first workflow

on:
  push:
    branches:
      - main

jobs:
  example-jobs:
    runs-on: ubuntu-latest

    steps:
      - name: print welcome
        run: echo "hello menna"

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: list repo files
        run: ls -la
